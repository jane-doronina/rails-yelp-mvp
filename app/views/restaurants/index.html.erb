<div class="container">
  <h1 class="mt-5 mb-4">Restaurants in London</h1>
  <ul>
    <% @restaurants.each do |resto| %>
    <li><%= link_to resto.name, restaurant_path(resto) %></li>
    <% end %>
  </ul>


</div>
